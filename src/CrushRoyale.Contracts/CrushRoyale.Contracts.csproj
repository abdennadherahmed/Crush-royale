<Project Sdk="Microsoft.NET.Sdk">

  <!-- HTTP API contracts shared by the ASP.NET server and the Unity client (plain POCOs, no serializer attributes). -->
  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <LangVersion>9.0</LangVersion>
    <Nullable>disable</Nullable>
    <RootNamespace>CrushRoyale.Contracts</RootNamespace>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

</Project>
